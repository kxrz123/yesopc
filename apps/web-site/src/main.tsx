import React from 'react';
import ReactDOM from 'react-dom/client';

const root = document.getElementById('root');

if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      {/* 预留：后续可以把整个官网改写为 React 组件 */}
      <></>
    </React.StrictMode>,
  );
}

